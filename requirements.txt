# AI 小说创作工具 - 依赖文件
# 版本：v2.0.0
# 作者：幻城科技

# =============================================================================
# 核心依赖
# =============================================================================

# Gradio Web UI 框架 - 主界面
gradio>=4.19.0,<5.0.0

# OpenAI 客户端 - API 调用
openai>=1.30.0,<2.0.0

# 数据处理和分析
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0

# =============================================================================
# 文档处理依赖
# =============================================================================

# Word 文档处理 (.docx)
python-docx>=0.8.11,<1.0.0

# PDF 文档处理
PyMuPDF>=1.23.0,<2.0.0

# EPUB 电子书处理
ebooklib>=0.18,<1.0.0

# HTML/XML 解析 (EPUB 处理需要)
beautifulsoup4>=4.12.0,<5.0.0

# Markdown 处理
markdown>=3.5.0,<4.0.0

# =============================================================================
# 数据处理依赖
# =============================================================================

# 已经包含 pandas 和 numpy

# 日期时间处理
python-dateutil>=2.8.0,<3.0.0

# 正则表达式增强 (可选)
regex>=2023.0.0,<2024.0.0

# =============================================================================
# 网络请求依赖
# =============================================================================

# HTTP 请求库
requests>=2.31.0,<3.0.0

# 异步 HTTP 客户端
httpx>=0.25.0,<1.0.0

# =============================================================================
# 配置管理依赖
# =============================================================================

# YAML 配置文件支持
pyyaml>=6.0.0,<7.0.0

# 环境变量管理
python-dotenv>=1.0.0,<2.0.0

# =============================================================================
# 日志和监控依赖
# =============================================================================

# 高级日志库
loguru>=0.7.0,<1.0.0

# 系统监控
psutil>=5.9.0,<6.0.0

# =============================================================================
# 开发依赖 (可选)
# =============================================================================

# 代码格式化
black>=23.0.0,<24.0.0

# 代码检查
flake8>=6.0.0,<7.0.0

# 单元测试框架
pytest>=7.4.0,<8.0.0

# 测试覆盖率
pytest-cov>=4.1.0,<5.0.0

# 类型检查
mypy>=1.5.0,<2.0.0

# =============================================================================
# 可选依赖
# =============================================================================

# 图像处理 (如果需要支持图片)
Pillow>=10.0.0,<11.0.0

# 进度条
tqdm>=4.65.0,<5.0.0

# 缓存支持 (可选)
cachetools>=5.3.0,<6.0.0

# =============================================================================
# 平台特定依赖
# =============================================================================

# Windows 特定 (如果需要)
# pywin32>=306

# macOS 特定 (如果需要)
# psutil 已经包含

# Linux 特定 (如果需要)
# psutil 已经包含

# =============================================================================
# 版本兼容性说明
# =============================================================================
# - 所有包都指定了最小和最大版本范围，确保兼容性
# - 核心依赖版本较新，确保功能完整
# - 可选依赖标记为可选，用户可以按需安装
# - 开发依赖单独放在 requirements-dev.txt 中

# =============================================================================
# 安装说明
# =============================================================================
# 基础安装:
# pip install -r requirements.txt
#
# 开发环境安装:
# pip install -r requirements.txt -r requirements-dev.txt
#
# 可选依赖安装:
# pip install Pillow tqdm cachetools
#
# 使用虚拟环境推荐:
# python -m venv venv
# source venv/bin/activate  # Linux/macOS
# venv\Scripts\activate     # Windows
# pip install -r requirements.txt